import { makeStyles } from '@material-ui/core'

export default makeStyles({
  backCatalog: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'flex-start',
    marginTop: '45px',
    cursor: 'pointer'
  }
})